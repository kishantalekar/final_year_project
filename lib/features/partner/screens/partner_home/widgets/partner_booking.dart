import 'package:flutter/cupertino.dart';

class PartnerBookingScreen extends StatelessWidget {
  const PartnerBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
